---
'boltdocs': patch
---

Rebrand the CLI output (dev/preview server boxes, build summary, doctor, update prompt) from the former terracotta brand color to an azure-blue accent (`#3d8bfa`), matching the updated primary palette. The `TERRACOTTA`/`TERRACOTTA_DEEP` exports in `ui-utils` are renamed to `BRAND`/`BRAND_DEEP`.
