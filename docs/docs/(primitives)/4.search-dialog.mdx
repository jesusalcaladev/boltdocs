---
title: "Search Dialog"
description: "High-performance search modal primitive with FlexSearch integration."
---

import { Field, Note, Tip, Table } from 'boltdocs/client'

# Search Dialog

The `SearchDialog` is a complex primitive that handles the search modal, input field, and the list of results. It is built on top of [FlexSearch](https://github.com/nextapps-de/flexsearch) for lightning-fast, client-side searching.

---

## Architecture

The search functionality is split into the visual dialog primitives and the `useSearch` hook which manages the indexing and filtering logic.

```tsx
import { SearchDialogPrimitive } from 'boltdocs/client'
```

---

## API Reference

### SearchDialogPrimitive.Root
```tsx
import { SearchDialogPrimitive } from 'boltdocs/client'
import { useSearch, useRoutes } from 'boltdocs/client'
import { useNavigate } from 'react-router-dom'

function CustomSearch() {
  const { routes } = useRoutes()
  const search = useSearch(routes)
  const navigate = useNavigate()

  return (
    <SearchDialogPrimitive.Root
      isOpen={search.isOpen}
      onOpenChange={search.setIsOpen}
    >
      <SearchDialogPrimitive.Autocomplete
        onSelectionChange={(key) => {
          if (key) {
            navigate(String(key))
            search.setIsOpen(false)
          }
        }}
      >
        <SearchDialogPrimitive.Input {...search.input} />

        <SearchDialogPrimitive.List items={search.list}>
          {(item) => (
            <SearchDialogPrimitive.Item
              key={item.id}
              id={item.path}
              textValue={item.title}
            >
              <SearchDialogPrimitive.Item.Icon isHeading={item.isHeading} />
              <div className="flex flex-col min-w-0 flex-1">
                <SearchDialogPrimitive.Item.Title>{item.title}</SearchDialogPrimitive.Item.Title>
                {item.bio && (
                  <SearchDialogPrimitive.Item.Bio>{item.bio}</SearchDialogPrimitive.Item.Bio>
                )}
              </div>
            </SearchDialogPrimitive.Item>
          )}
        </SearchDialogPrimitive.List>
      </SearchDialogPrimitive.Autocomplete>
    </SearchDialogPrimitive.Root>
  )
}
```
