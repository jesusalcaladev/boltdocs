---
title: "Button"
description: "A versatile button component for internal and external links."
icon: MousePointer2
---

import { Button, Table, Field } from 'boltdocs/client'

# Button

A versatile button/link component for use in MDX pages and the home page.

**Example:**

<div style={{ display: 'flex', gap: '1rem', flexWrap: 'wrap' }}>
  <Button variant="primary" size="md">
    Get Started
  </Button>

  <Button variant="secondary" size="md">
    Click Me
  </Button>
  
  <Button variant="outline" size="md">
    Outline
  </Button>
</div>

```tsx
<Button variant="primary" size="md">
  Get Started
</Button>

<Button variant="secondary" size="md" >
  Click Me
</Button>

<Button variant="outline" size="md">
  Outline
</Button>
```

**Props (`ButtonProps`)**

<Field name="variant" type='"primary" | "secondary" | "outline" | "ghost"' defaultValue='"primary"'>
  The visual style variant of the button.
</Field>

<Field name="size" type='"sm" | "md" | "lg"' defaultValue='"md"'>
  The sizing scale of the button.
</Field>

<Field name="href" type="string">
  If provided, the element renders as an `<a>` tag instead of a `<button>`.
</Field>

_(Standard anchor and button HTML attributes are also supported)_

## Personalization (CSS)

Override these variables in your `custom.css` to restyle all buttons globally:

```css
:root {
  --ld-btn-primary-bg: #7f13ec;
  --ld-btn-primary-text: #ffffff;
  --ld-btn-primary-hover: #6b0fcc;
  --ld-btn-secondary-bg: rgba(255, 255, 255, 0.06);
  --ld-btn-secondary-text: #e0e0e0;
  --ld-btn-secondary-hover: rgba(255, 255, 255, 0.12);
  --ld-btn-outline-border: var(--ld-border-strong);
  --ld-btn-outline-text: var(--ld-text-main);
}
```
