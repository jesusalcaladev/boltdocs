---
title: "Button"
description: "A versatile button component for internal and external links."
icon: MousePointer2
---

import {ArrowRight, Github, ArrowLeft, ExternalLink} from 'lucide-react'

# Button

The Button component is a primary interaction element used for navigation, form submission, and call-to-actions. It automatically handles internal routing via React Router or renders as a standard anchor tag for external links.

## Props Table

<ComponentProps
  props={[
    {
      name: "variant",
      type: '"primary" | "secondary" | "outline" | "ghost" | "danger" | "success" | "warning" | "info" | "subtle" | "link"',
      defaultValue: '"primary"',
      description: "The visual style variant of the button."
    },
    {
      name: "size",
      type: '"sm" | "md" | "lg"',
      defaultValue: '"md"',
      description: "The physical size and padding scale."
    },
    {
      name: "color",
      type: 'string',
      description: "Custom color theme (e.g., 'purple', 'cyan', 'pink', 'orange', 'blue', 'red', 'green')."
    },
    {
      name: "rounded",
      type: '"none" | "sm" | "md" | "lg" | "full"',
      defaultValue: '"md"',
      description: "The border radius maturity."
    },
    {
      name: "href",
      type: "string",
      description: "Destination URL. Local paths use internal routing; others render as standard links."
    },
    {
        name: "icon",
        type: "ReactNode",
        description: "Optional icon element to display alongside the text."
    },
    {
        name: "iconPosition",
        type: '"left" | "right"',
        defaultValue: '"left"',
        description: "The position of the icon relative to the text."
    }
  ]}
/>

---

## Variants & Examples

### Basic Usage

Use the `<Button>` component directly in your MDX. It defaults to the `primary` variant and `md` size.

<Button>Primary Action</Button>

### Semantic Variants

Buttons come in several semantic variants to help establish intent and hierarchy.

<div style={{ display: 'flex', gap: '1rem', flexWrap: 'wrap' }}>
  <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="success">Success</Button>
  <Button variant="danger">Danger</Button>
  <Button variant="warning">Warning</Button>
  <Button variant="info">Info</Button>
  <Button variant="subtle">Subtle</Button>
  <Button variant="ghost">Ghost</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="link">Link</Button>
</div>

### Custom Colors

You can use the `color` prop to apply specific brand colors with neon glow effects.

<div className="flex flex-wrap gap-4 mt-6">
  <Button color="purple">Purple</Button>
  <Button color="cyan">Cyan</Button>
  <Button color="pink">Pink</Button>
  <Button color="orange">Orange</Button>
  <Button color="blue">Blue</Button>
</div>

### Icons

Add icons for better visual recognition. You can place them on the left or right.

<div className="flex flex-wrap gap-4 mt-6">
  <Button icon={<ArrowLeft />}>Go Back</Button>
  <Button icon={<ArrowRight />} iconPosition="right">Next Step</Button>
  <Button variant="outline" icon={<Github />}>Connect GitHub</Button>
  <Button variant="ghost" icon={<ExternalLink />} iconPosition="right">Open App</Button>
</div>

### Rounded Corners

Control the corner radius using the `rounded` prop.

<div className="flex flex-wrap gap-4 mt-6">
  <Button rounded="none">None</Button>
  <Button rounded="sm">Small</Button>
  <Button rounded="md">Medium</Button>
  <Button rounded="lg">Large</Button>
  <Button rounded="full">Full</Button>
</div>

### Tailwind CSS v4 Support

You can use Tailwind utility classes directly in your MDX for highly custom designs.

<div className="mt-8 p-6 rounded-2xl bg-gradient-to-br from-indigo-500/10 to-purple-500/10 border border-white/5 backdrop-blur-sm">
  <h4 className="text-xl font-semibold text-white mb-2">Custom Styled Card</h4>
  <div className="text-zinc-400 mb-4">
    This card is styled entirely with <strong>Tailwind CSS v4</strong> utility classes.
  </div>
  <Button className="bg-sky-500 hover:bg-sky-400 transition-all hover:scale-105 px-8">
    Tailwind Styled Button
  </Button>
</div>

### Personalization (CSS)

Customize the appearance of all buttons globally using these CSS variables in your `custom.css`:

```css
:root {
  /* Primary Button */
  --ld-btn-primary-bg: #7f13ec;
  --ld-btn-primary-text: #ffffff;
  --ld-btn-primary-glow: rgba(127, 19, 236, 0.3);

  /* Secondary Button */
  --ld-btn-secondary-bg: rgba(255, 255, 255, 0.06);
  --ld-btn-secondary-text: #e0e0e0;
  
  /* Global Radius */
  --ld-radius-md: 8px;
}
```
